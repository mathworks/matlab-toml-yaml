function obj = addKey(obj, key) %#ok<INUSD>
%ADDKEY No-op retained for parser compatibility.
%   Key order is maintained by the dictionary's insertion order.
%   Parsers should use setData to add keys with values.
end
