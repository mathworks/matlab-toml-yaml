classdef (Abstract) ConfigurationDescribe < matlab.io.config.internal.ConfigurationStorage
    %CONFIGURATIONDESCRIBE Schema inspection via describe()

    methods
        result = describe(obj, options)
    end
end
